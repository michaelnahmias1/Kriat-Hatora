# קריאת התורה — כתוביות מסונכרנות

הפקת קובץ כתוביות (SRT) עם טקסט מקראי **מנוקד ומוטעם**, מסונכרן להקלטת קריאה בטעמים,
לייבוא ל-CapCut. הערך של המערכת הוא הטקסט המדויק — לא התזמון (שכלי עריכה כבר עושים, אבל
תוך שיבוש העברית המקראית). נתמכים **כל 39 ספרי התנ״ך** — תורה, נביאים וכתובים.

## תמיכה בכל התנ״ך — מה מיוחד מעבר לתורה

- **רשימת הספרים** חיה במקום אחד: `src/chunker/books.py` (עברית ← שמות
  Sefaria באנגלית) ומשרתת את ה-API, את הצינור ואת ולידציית ה-worker;
  תפריט הספרים ב-`index.html` נבנה מאותה רשימה (ויש בדיקה שמוודאת התאמה).
- **טעמי אמ״ת** (תהלים, משלי, איוב): מערכת טעמים שונה עם היררכיה משלה —
  לחיתוך נוספה טבלת דרגות שנייה (`taamim.RANK_OF_EMET`). הזיהוי **פר-פסוק**
  לפי סימנים ייחודיים לאמ״ת (עולה, דחי, עילוי, אתנח הפוך), מה שמטפל
  אוטומטית גם באיוב, שהמסגרת הסיפורית שלו בטעמי כ״א ספרים.
- **קרי/כתיב** (נפוץ מאוד בנביאים ובכתובים): ב-MAM הכתיב מודפס בלי ניקוד
  לצד הקרי המנוקד. מילה בלי שום ניקוד מזוהה ככתיב — נשארת בתצוגה (נאמנות
  למקור) אך לא נכנסת לרשימות היישור, כי הקורא אינו הוגה אותה.
- **קטעים בארמית** (דניאל ב–ז, עזרא ד–ו): הטקסט והחיתוך עובדים כרגיל
  (אותם טעמים), והיישור המאולץ (MMS + רומניזציה) אגנוסטי לשפה; רק שכבת
  העוגנים (תמלול עברי) צפויה לתרום פחות שם — הצינור ממילא בנוי להמשיך
  גם עם כיסוי עוגנים נמוך, ודוח האיכות יסמן מקטעים לבדיקה ידנית.
- **גרסת הטקסט**: ‏MAM מכסה את כל התנ״ך; ליתר ביטחון, אם לספר כלשהו לא
  תימצא גרסת מסורה — נופלים אוטומטית לגרסה עברית מוטעמת אחרת.

**התוכנית המלאה: [PLAN.md](PLAN.md)** ‏(v6 — הפייפליין ההיברידי).

## הממשק הקבוע — הכל מהטלפון, בלי Colab

**‏https://kriat-hatora.vercel.app**

1. **בחר קטע** — ספר, פרק, טווח פסוקים. הטקסט (גרסת MAM, מנוקד ומוטעם) נטען מ-Sefaria
   ונחתך למקטעים לפי טעמי המקרא.
2. **בחר את ההקלטה** מהטלפון — הקובץ נשאר במכשיר (מועלה רק אם בוחרים בתזמון הענן).
3. **תזמן**:
   - **🛰️ תזמון AI בענן** — המסלול ההיברידי המלא (תמלול ← עוגנים ← יישור ← VAD)
     רץ על GPU בענן ישירות מהאפליקציה, בלי Colab; הזמנים ודוח האיכות חוזרים
     ישר לשלב הבדיקה. (דורש פריסה חד-פעמית של ה-worker — ראה בהמשך.)
   - **⚡ תזמון אוטומטי במכשיר** — ניתוח ההקלטה בדפדפן (`autotime.js`), בלי העלאה.
   - תזמון ידני בהקשות וחלוקה לפי אורך נשארו כגיבוי.
4. **בדוק ותקן** — נגינה מכל נקודת כניסה, כוונון של ±0.2 שניות.
5. **ייצא SRT** (הורדה / שיתוף / העתקה) ← CapCut Web ← Cloud ← אפליקציית CapCut ← פרסום.

הממשק רץ על Vercel (פרויקט `kriat-hatora`): דף סטטי + פונקציית Python אחת
(`api/segments.py`) שמושכת את הטקסט מ-Sefaria וחותכת אותו עם `src/chunker`.
**אין צורך באחסון ענן** (Supabase וכד') — עיבוד הענן הוא job חולף שלא שומר כלום.

## מסלול היישור המדויק (AI) — הפייפליין ההיברידי

המסלול שנותן את הסנכרון הטוב ביותר, בשלוש שכבות שכל אחת מפצה על חולשת קודמתה:
תמלול עברי (ivrit.ai) שמוצלב עם טקסט Sefaria ומייצר "עוגנים", יישור כפוי (MMS)
על חלונות קצרים בין העוגנים, והצמדת גבולות לנשימות (VAD). לצד ה-SRT נוצר דוח
איכות שמפרט אילו מקטעים בודדים כדאי לגרור ידנית.

### בתוך האפליקציה (Modal) — המסלול הראשי

הצינור רץ כ-worker ב-[Modal](https://modal.com) ‏(GPU T4 לפי-שימוש; התוכנית
החינמית כוללת קרדיט חודשי שמכסה שימוש אישי בשפע). האפליקציה שולחת את ההקלטה
ואת הפרמטרים ל-endpoint, שואלת לשלום העבודה כל כמה שניות, ובסיום מזרימה את
הזמנים ודוח האיכות ישר לשלב הבדיקה. **הקוד ב-`src/aligner` זהה לזה של Colab —
אותה משיכת טקסט מ-Sefaria ואותה שיטת עיבוד בדיוק.**

**עיבוד ברקע:** צריך להשאיר את האפליקציה פתוחה רק עד סיום ההעלאה (יש מד
אחוזים, והמסך נשאר דולק אוטומטית). מרגע שההעלאה הושלמה העבודה רצה בענן
בלי שום תלות בטלפון — אפשר לסגור את האפליקציה לגמרי; מזהה העבודה נשמר
במכשיר (`localStorage`), ובכניסה הבאה התוצאה נמשכת אוטומטית (Modal שומר
תוצאות כשבוע). כפתור הביטול עוצר את העבודה גם בענן. גם הקלטות ארוכות
(40 דק' ויותר) מכוסות — מגבלת הריצה של ה-worker היא שעתיים.

### התראה בסיום העיבוד (Web Push — הקמה חד-פעמית, לא חובה)

כשמפתחות VAPID מוגדרים, המשתמש מקבל התראה בסיום העיבוד (הצלחה או כישלון)
**גם כשהאפליקציה סגורה** — ה-worker שולח Web Push ישירות למכשיר, בלי שום
אחסון בענן (ה-subscription נוסע יחד עם העבודה). בלי המפתחות הכל עובד כרגיל,
רק בלי התראות. הקמה:

1. יצירת זוג מפתחות (פעם אחת, מכל מחשב): `npx web-push generate-vapid-keys`
2. מוסיפים ל-secret‏ `kriat-hatora` הקיים ב-Modal‏ (בדשבורד: Secrets ←
   ‏kriat-hatora ← עריכה, או ב-CLI עם `modal secret create kriat-hatora --force`
   תוך חזרה על `KRIAT_HATORA_TOKEN`) את שני המפתחות:
   - `VAPID_PRIVATE_KEY` — המפתח הפרטי
   - `VAPID_SUBJECT` — ‏`mailto:` וכתובת המייל שלך (למשל `mailto:me@example.com`)
3. מדביקים את המפתח **הציבורי** בקבוע `VAPID_PUBLIC_KEY` שבראש `index.html`.
4. פורסים מחדש: את ה-worker (דחיפה ל-main מפעילה את ה-workflow, או
   `modal deploy modal_worker/app.py`) ואת האתר (Vercel — אוטומטי מהדחיפה).

באנדרואיד ההתראות עובדות גם כשהדפדפן סגור; באייפון (iOS 16.4+) — רק
כשהאפליקציה מותקנת למסך הבית (יש כפתור התקנה בהגדרות האפליקציה).

פריסה חד-פעמית (ממחשב, עם חשבון Modal — הרשמה עם GitHub):

```bash
pip install modal
modal setup                                              # התחברות (פעם אחת)
modal secret create kriat-hatora KRIAT_HATORA_TOKEN=<מחרוזת-אקראית-ארוכה>
modal deploy modal_worker/app.py                         # מדפיס את כתובת ה-web
```

ואז ממלאים ב-`index.html` את שני הקבועים שבראש הסקריפט — `AI_ENDPOINT` (הכתובת
שהודפסה, של הפונקציה `web`) ו-`AI_TOKEN` (אותה מחרוזת מה-secret) — ופורסים ל-Vercel
כרגיל. כל עוד הקבועים ריקים הכפתור פשוט מוסתר והאפליקציה עובדת כמו קודם.

הערה: ה-token חשוף בצד הלקוח — זו הגנת-עלות בסיסית מפני שימוש אקראי בכתובת,
לא סוד אמיתי. מתאים לכלי אישי; אם הכלי ייפתח לציבור, נוסיף שער אמיתי.

**חשוב — שתי פריסות נפרדות:** דחיפה ל-GitHub מעדכנת אוטומטית רק את האתר
(Vercel). ה-worker בענן **לא** מתעדכן מדחיפה — בלי פריסה מחדש הוא ממשיך
להריץ את הקוד הישן, גם כששינוי כבר נראה באתר (כך שגיאת ה-torchcodec חזרה
אחרי שכבר תוקנה בקוד). שתי דרכים לעדכן אותו:

- **אוטומטית (מומלץ):** ה-workflow‏ `.github/workflows/deploy-modal.yml` פורס
  ל-Modal בכל שינוי של `src/` או `modal_worker/` שמגיע ל-main. הפעלה
  חד-פעמית: יוצרים token ב-Modal‏ (Settings ← API Tokens, או `modal token new`)
  ומוסיפים בריפו ב-GitHub‏ (Settings ← Secrets and variables ← Actions) את
  שני ה-secrets‏ `MODAL_TOKEN_ID` ו-`MODAL_TOKEN_SECRET`.
- **ידנית:** מריצים שוב `modal deploy modal_worker/app.py` מהמחשב אחרי כל
  שינוי בצינור.

### ב-Colab (גיבוי)

אותו מעבד בדיוק, למקרה שהענן לא זמין:

1. **פתח את המעבד ב-Colab** (יש קישור גם בתחתית האפליקציה):

   [![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/michaelnahmias1/Kriat-Hatora/blob/main/notebooks/worker.ipynb)

   בפעם הראשונה: ‏Runtime ← Change runtime type ← **T4 GPU**.
2. **מלא את תא הפרמטרים** (ספר, פרק, טווח פסוקים) ← Runtime ← **Run all** ←
   כשתא 2 מבקש, **בחר את קובץ ההקלטה ישירות מהטלפון** (לא צריך Google Drive;
   מי שבכל זאת עובד מ-Drive — ממלא נתיב בתא 1 כמו קודם).
3. ה-SRT ודוח האיכות מוצעים להורדה ישירה לטלפון, וייבוא ה-SRT בשלב 3 באפליקציה
   מכניס את הזמנים לשלב הבדיקה.

## מה יש בריפו

| נתיב | מה זה |
|---|---|
| `PLAN.md` | תוכנית הפעולה המחייבת (v6) |
| `index.html` | **הממשק הקבוע** — דף אחד, עברית, מותאם לטלפון (נפרס ל-Vercel) |
| `autotime.js` | מנוע התזמון בדפדפן — ניתוח ההקלטה במכשיר, בלי שרת ובלי העלאה |
| `api/segments.py` | פונקציית ה-API: ‏Sefaria ← ניקוי ← חיתוך ← JSON (Vercel Python) |
| `vercel.json` | תצורת הפריסה |
| `modal_worker/` | **ה-worker בענן (Modal)** — עוטף את `run_hybrid` כ-endpoint ‏(submit/status) על GPU לפי-שימוש |
| `notebooks/worker.ipynb` | מסלול ה-AI ב-Colab (גיבוי) — אותו צינור, מופעל ידנית מ-notebook |
| `notebooks/poc_eval.ipynb` | הערכה: סימון ground truth והשוואת שיטות הסנכרון |
| `notebooks/shelav0_verification.ipynb` | notebook אבחון: אימות טקסט/טעמים/קרי-כתיב/הקלטה |
| `src/chunker/` | חיתוך למקטעים לפי טעמי המקרא — Python טהור, אפס תלויות (משרת את שני המסלולים) |
| `src/aligner/` | הצינור ההיברידי ל-Colab: ‏Sefaria ← חיתוך ← תמלול+עוגנים ← יישור ממושכן ← VAD ← SRT |
| `tests/` | בדיקות יחידה (רצות בלי רשת): ‏Python לצינור ול-API, ‏Node למנוע הדפדפן |

## פריסה מחדש (למפתחים)

הפריסה נעשית לפרויקט `kriat-hatora` ב-Vercel (לא לגעת בפרויקטים אחרים בחשבון).
הקבצים שנפרסים: `index.html`, ‏`autotime.js`, ‏`api/segments.py`, ‏`vercel.json`, ‏`src/chunker/`.

## הרצת הבדיקות (למפתחים)

```bash
python3 -m unittest discover -s tests -v   # החיתוך וה-API
node tests/test_autotime.mjs               # מנוע התזמון האוטומטי
```
